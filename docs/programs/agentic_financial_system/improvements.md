# Agentic Financial System Improvement Backlog

## Immediate

- define a structured schema for first-party and client-authorized consent records;
- design the inbox-to-CRM workflow around summarization, tagging, and handoff rather than contact extraction;
- map one public-data listing niche with clear source terms and monetization paths;
- create a reusable offer template for automation services sold to small businesses.

## Near-term

- add a journal append flow so each cron run records score changes and lane decisions;
- expose `revenue_planning` inside the UI as a guided decision helper;
- add a small library of compliant revenue playbooks and counterexamples;
- track blocked ideas so the agent does not repeatedly revisit rejected personal-data schemes.

## Validation

- require a PASS from `score.sh` before execution;
- require a documented customer, problem, and acquisition method before launch;
- review every personal-data-touching lane against `docs/policies/compliance_pack.md`.
